import { Box } from "@mui/material";
import Navbar from "../components/common/Navbar";

const Select = () => {
  return (
    <Box sx={{ display: "flex" }}>
      <Navbar />
      <h1>Select</h1>
    </Box>
  );
};

export default Select;
